{
  "name": "Token Manager",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "server",
      "command": "cd server && MONGO_URI=mongodb://127.0.0.1:27017 MONGO_DB=tokenman PORT=8081 npm start",
      "description": "Express API server on http://localhost:8081 (connects to local MongoDB)."
    },
    {
      "name": "client",
      "command": "cd client && VITE_API_URL=http://localhost:8081 npm run dev",
      "description": "Vite dev server for the Vue client on http://localhost:5173."
    }
  ],
  "ports": [
    { "name": "client", "port": 5173 },
    { "name": "api", "port": 8081 }
  ]
}
